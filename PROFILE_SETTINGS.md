# Profile settings to change by hand

These live in GitHub's UI, not in this repo. Both currently contradict the resume.

## Bio

Settings → Public profile → Bio (160 char limit). Current text is
"Hello, its Bilal here. I am a learner and have a great interest in coding."

Replace with one of:

```
Senior AI Engineer — LangChain/LangGraph, RAG, multi-agent systems. Building production LLM infrastructure. Remote contract.
```

```
Senior AI Engineer. Agentic systems, retrieval pipelines, FastAPI backends. 6+ years. Remote contract only.
```

## Location

Currently "Lahore, Pakistan" — the resume says Oshawa, ON. Set it to `Oshawa, Ontario, Canada`
or clear it. An inconsistency here is the kind of thing that stalls a client conversation.

## Pinned repositories

Profile → Customize your pins. Pins override the "Popular repositories" auto-sort,
which currently surfaces coursework. Pin in this order:

1. `rag-pipeline-fastapi`
2. `langgraph-supervisor-rollback`
3. `llm-gateway-observability`
4. `Genetic-Algorithm` (optional — keeps some CS fundamentals visible)

Leave the rest unpinned. Don't delete them; unpinned is enough.

## Website field

Currently `www.codewithbilal.com`. Worth checking it still resolves and tells the
same story as the resume before leaving it linked.

## Repo topics

On each new repo, add topics so they surface in search:
`llm`, `rag`, `langchain`, `langgraph`, `fastapi`, `ai-agents`, `prometheus`, `python`.
